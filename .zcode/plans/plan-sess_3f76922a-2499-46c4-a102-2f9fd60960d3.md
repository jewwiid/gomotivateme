1. **New template** `emails/goalCreated.tsx` + register in `emails/render.tsx`
2. **Enqueue** it in `convex/goals.ts` `create` mutation (after insert, before return) — `category: "transactional"`
3. **Fix** broken invite unsubscribe link: remove `unsubscribeUrl` from `emails/inviteReceived.tsx`
4. **Set** `RESEND_API_KEY` on the Convex deployment with the key you provided
5. **Typecheck** and verify