## Already implemented — no work needed

This feature is fully built and deployed. I verified every layer:

**Follower notification system (`convex/goals.ts:959-1166`)**
- `collectNotifiableFollowers` queries BOTH motivators AND supporters, dedupes by userId, resolves emails, gates by per-tier prefs
- Supporters gate on `supportedGoalUpdates`, motivators gate on `yourMotivations`, passes if either is on for someone who is both
- `notifyFollowersOfUpdate`, `notifyFollowersOfCompletion`, `notifyFollowersOfStatusChange` all consume it

**Fan-out triggers (all wired)**
- `recordValue` — fires on progress log (line 932)
- `toggleMilestone` — fires on completion (line 636)
- `logStreakDay` — fires on streak day (line 817)
- `setStatus` — fires on pause/close (line 526)
- Moderation approval — fires when text update is approved (`moderation.ts:96,147`)

**Preferences**
- `supportedGoalUpdates` field exists in schema + defaults (true) + update mutation
- Settings toggle "Updates on goals you support" rendered in NotificationsTab

**Suppression**
- Uses `category: "lifecycle"` so `unsubscribedAll` works
- `enqueue` injects unsubscribe token for footer link

When a supporter clicks "Support this goal" and the owner later posts progress, the supporter gets a "new update" email — exactly the follow system described. Nothing to build.